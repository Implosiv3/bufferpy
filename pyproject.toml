[project]
name = "bufferpy"
version = "0.1.1"
description = "Buffer API"
authors = [
    {name = "danialcala94",email = "danielalcalavalera@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.8,<3.14"

[project.urls]
Homepage = "https://github.com/Implosiv3/bufferpy"
Issues = "https://github.com/Implosiv3/bufferpy/issues"

[tool.poetry.dependencies]
# Mandatory
requests = { version = ">=2.32.4,<9999.0.0", optional = false }
# Optional

[tool.poetry]
packages = [{include = "bufferpy", from = "src"}]

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
env_easy = ">=0.0.3,<1.0.0"

[tool.pytest.ini_options]
markers = [
    "mandatory: mandatory tests for release",
    "additional: exhaustive and demanding tests"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
